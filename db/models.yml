Movie:
  name: string
  image: string
  summary: text
  year_released: string
  rating: string
  runtime: string
  format: string
  price: integer
  studio_id: integer
  created_at: datetime
  updated_at: datetime

Studio:
  name: string

Actor:
  name: string
  image: text

Genre:
  name: string

Categorization:
  movie_id: integer
  genre_id: integer

Role:
  movie_id: integer
  actor_id: integer
  character_name: string

User:
  name: string
  email: string
  password_digest: string
  role: string